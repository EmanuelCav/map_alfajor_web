export interface IAlfajor {
    id: string;
    alfajor: string;
}

export interface IAlfajorSelected {
    id: string;
    alfajor: string;
    isSelected: boolean;
}

export interface ICreateAlfajor {
    alfajor: string;
}